{
  "name": "collabcanvas-functions",
  "version": "1.0.0",
  "description": "CollabCanvas AI Agent Cloud Functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "ai": "^3.4.33",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0",
    "openai": "^4.28.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "private": true
}
